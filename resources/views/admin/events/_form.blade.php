<div class="field"><label for="title">Title *</label><input id="title" name="title" value="{{ old('title', $event->title ?? '') }}" maxlength="255" required></div>
<div class="field"><label for="description">Description *</label><textarea id="description" name="description" rows="8" required>{{ old('description', $event->description ?? '') }}</textarea></div>
<div class="form-grid"><div class="field"><label for="date_or_period">Date or period</label><input id="date_or_period" name="date_or_period" value="{{ old('date_or_period', $event->date_or_period ?? '') }}" maxlength="255"></div><div class="field"><label for="location">Location</label><input id="location" name="location" value="{{ old('location', $event->location ?? '') }}" maxlength="255"></div></div>
<div class="field"><label for="sort_year">Timeline start year</label><input id="sort_year" type="number" name="sort_year" value="{{ old('sort_year', $event->sort_year ?? '') }}" min="-5000" max="3000"><small>Used only for chronological sorting. Enter negative values for BCE dates (for example, -300).</small></div>
<div class="field"><label for="image">Image</label><input id="image" type="file" name="image" accept="image/*"><small>Maximum 2 MB. Uploading a new image replaces the current one.</small></div>
@isset($event) @if($event->image)<img class="form-preview" src="{{ asset('storage/' . $event->image) }}" alt="Current event image"><label class="check"><input type="checkbox" name="remove_image" value="1" @checked(old('remove_image'))> Remove current image</label>@endif @endisset
<div class="actions"><button class="button" type="submit">{{ $submitLabel }}</button><a class="button secondary" href="{{ route('admin.events.index') }}">Cancel</a></div>
